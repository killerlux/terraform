name: Deploy Private AI Stack to DigitalOcean

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      droplet_ip: ${{ steps.terraform_apply.outputs.droplet_ip }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0

      - name: Authenticate to DigitalOcean & Run Terraform
        id: terraform_apply
        run: |
          cd terraform
          terraform init
          terraform apply -auto-approve -var="do_token=${{ secrets.DIGITALOCEAN_TOKEN }}"
          
          DROPLET_IP=$(terraform output -raw droplet_ip_address | tail -n 1)
          echo "droplet_ip=$DROPLET_IP" >> $GITHUB_OUTPUT

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy Docker Compose file to Droplet
        uses: appleboy/scp-action@master
        with:
          host: ${{ needs.build.outputs.droplet_ip }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.yml"
          target: "/root"
          
      - name: Deploy Docker Stack on Droplet
        uses: appleboy/ssh-action@master
        with:
          host: ${{ needs.build.outputs.droplet_ip }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            export N8N_HOST=${{ needs.build.outputs.droplet_ip }}
            
            cd /root
            echo "Starting docker services..."
            docker-compose up -d
            
            echo "Waiting for services to start..."
            sleep 30

            echo "Pulling Llama3 model in Ollama..."
            docker exec ollama_service ollama pull llama3:8b
            echo "Deployment complete."
